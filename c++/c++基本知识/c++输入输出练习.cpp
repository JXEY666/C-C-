#include<iostream>
using namespace std;

/*
{
	int a;
	char s;
	cin>>a>>s;
	cout<<a<<std::endl;
	cout<<s<<endl;
	return 0;
} 
输入：std::cin>>
	  cin.get() 
	  cin.get(ch)=>gets
	  int main()
	{
		char ch;
		while((cin.get(ch))!=NULL)
		{
			cout<<ch;
		}
		return 0;
	}
	  cin.get(ch,20,'\n')=>fgets()
	  getline(cin,ch)将cin的内容赋给ch；
	   cin.clear;
*/
