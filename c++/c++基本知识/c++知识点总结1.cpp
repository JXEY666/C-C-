1.计算字符串长度：
#include<cstring>//#include<string.h>
{
	std::string s;//std::string s相当于char s[]; 
	int len=s.length() //int = s.size()
}

2.<<endl相当于C语言'\n';

3.c++可用C语言的标准函数库中的函数 

4.#include<utility>
 // 使用 std::swap 函数交换元素
std::swap(a[j], a[j + 1]);

5.

